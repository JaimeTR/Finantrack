/**
 * Este archivo contenía funciones Firebase para Firestore.
 * Ahora la app usa Supabase y estos métodos ya no se utilizan.
 * Se deja como referencia histórica; se pueden borrar después de confirmar la migración.
 */

// Archivo obsoleto tras migración a Supabase
export {};
